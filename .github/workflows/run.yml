on: [push]

jobs:
  custom_test:
    runs-on: ubuntu-latest
    name: RUN TESTING
    steps:
      - name: RUN TEST
        uses: pirasanthan-jesugeevegan/github-action-test@v2
        with:
          env: "dev"
        